  <%# <p>
    <strong>Commenter:</strong> %>
    <%#= @article.comments.user_id.name %>
  <%# </p> %>
  
    <p>
      Comment from <span class="fw-bold"><%= comment.user.name %></span>:
      <%= comment.content %>
    </p>
    
 

<div>
  <%#= link_to "Edit this comment", edit_article_comment_path(@article) %>
  
  <%#= link_to "Show this comment", article_comment_path(@article) %>


  <%#= link_to "Back to comments",  article_comments_path(@article) %>
  <% if user_signed_in? %>
    <% if current_user.role == "admin" %>
      <p>
        <%= link_to "Destroy Comment", [comment.article, comment], data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                    } %>
      </p>
    <% end %>
  <% end %>
</div>